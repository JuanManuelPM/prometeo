{
  "schema": "prometeo.universal-control-favorites-store-contract/v1",
  "status": "CANDIDATE_COMPATIBILITY_FACADE",
  "legacy_authority": {
    "storage": "localStorage",
    "key": "prometeo.v5.favorites.v1",
    "mode": "LEGACY_LOCALSTORAGE_COMPAT",
    "synchronous": true,
    "network_required": false
  },
  "observable_semantics": [
    "Favorites are an ordered array of stable page_id strings.",
    "Malformed JSON or a non-array legacy value reads as an empty list.",
    "Non-string and empty-string legacy entries are ignored.",
    "Duplicate legacy page_ids are deduplicated while preserving first-occurrence order.",
    "Pinning appends the page_id; unpinning removes that page_id.",
    "Reordering changes only order and persists synchronously.",
    "Catalog pruning removes unknown page_ids while preserving relative order of surviving ids and writes only when the list length changes.",
    "Constructing the facade performs no write, migration, deletion or network action."
  ],
  "golden_master_reference": {
    "payload_sha256": "67965c4da9161737f06fcf6503d6b73db43eef574796977327c97f165e514d8a",
    "snapshot": "shared/universal-shell/v5/candidate/baseline-source.html",
    "legacy_functions": [
      "readFavoriteIds",
      "saveFavoriteIds",
      "isFavorite",
      "toggleFavoritePage",
      "finishFavoriteDrag",
      "ensureCatalog"
    ]
  },
  "migration_rules": [
    "This facade is not a persistence cutover. localStorage remains authoritative in phase 1.",
    "Do not delete or rewrite the legacy key merely because a new storage backend exists.",
    "A future PrometeoDB migration must be additive, idempotent, rollback-safe and parity-verified before ownership changes.",
    "The Universal Control UI must not know whether the backend is legacy, shadow or canonical DB.",
    "Do not combine Favorites persistence migration with Corner Anchor, navigation, Capture, Notes, Context or visual refactors."
  ],
  "promotion_gates": [
    "exact legacy read normalization parity",
    "legacy toggle byte parity",
    "order-preserving reorder",
    "catalog-prune parity",
    "reload persistence",
    "offline operation",
    "served V5 remains unchanged until candidate browser parity passes"
  ]
}
