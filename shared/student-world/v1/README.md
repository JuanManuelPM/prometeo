# Student World Engine v1

Pure shared world/progression layer. It owns topology, prerequisites, unlocks and semantic current-node state.

It does **not** own map art, page layout, HUD, student theme or PageKit. Coordinates are a visual projection, not canonical progress state.
