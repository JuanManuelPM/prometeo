# PageKit Host v1

The host does not rebuild PageKit. It mounts an exact/version-pinned PageKit artifact in a persistent iframe and owns only lifecycle/focus boundaries.

- v26: positive rollback fossil.
- v37: capability/interactions candidate; not an automatic visual promotion.
- PageKit remains one service consumed by classes/tools.
