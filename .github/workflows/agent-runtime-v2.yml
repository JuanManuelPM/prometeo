name: Prometeo Agent Runtime v2 (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Agent Runtime v2 publisher is retired.'
          echo 'Runtime v3 owns derived agent-runtime publication.'
