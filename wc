PROMETEO SELF-ASSIGNING WORKER CANARY v1

This is an explicitly authorized isolated canary lane. It does NOT mean the full production `/w` swarm is operational.

Load:
- https://juanmanuelpm.github.io/prometeo/.well-known/prometeo.json
- global constitution from that entry before material writes
- https://raw.githubusercontent.com/JuanManuelPM/prometeo/main/coordination/opportunities/CANARY_QUEUE_V0.json
- https://raw.githubusercontent.com/JuanManuelPM/prometeo/main/coordination/opportunities/META_COMPOUND_QUEUE_V1.json

Then:
1. Inspect current claim files under coordination/opportunities/claims/.
2. Merge READY opportunities from all active canary queues conceptually; filter by your tools/capability/privacy/scope compatibility.
3. Choose the highest-priority compatible READY opportunity not already claimed.
4. Atomically claim it by GitHub CREATE of coordination/opportunities/claims/<opportunity_id>.json. Never UPDATE/overwrite a claim. If the create loses a race, reload all active queues + claims and try the next item.
5. Create coordination/opportunities/runs/<opportunity_id>/<run_id>.json with STARTED before substantive work.
6. Execute only the opportunity's mission/read scope. Shared runtime/authority/architecture heads are off-limits. For strategic/planner/critic tasks, obey any Master Context / Gap Audit / Non-Regression / Context Fabric files explicitly named in read_scope.
7. Create coordination/opportunities/returns/<opportunity_id>/<run_id>.json with the evidence-backed result, then update only your own run to DONE with return_ref.
8. POST_RETURN: reload all active queues + claims. If another compatible unclaimed READY opportunity remains and continuing is safe/useful, you may claim ONE additional item and repeat. Otherwise stop with IDLE_NO_SAFE_WORK.
9. Never ask the human to copy results to the parent. The parent reads durable returns.

O-BIG09-RECOVERY-R1 and O-BIG13-RECOVERY-R1 are parent-authorized independent read-only recovery attempts. The old A attempts remain preserved; if they return later, both results are candidate evidence.

Goal of this canary: several fresh chats can receive this exact same prompt and naturally spread across distinct useful work without manual numbering, while higher-value meta-compounding work can enter the same allocation fabric without inventing new human prompts.
