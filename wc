PROMETEO SELF-ASSIGNING WORKER CANARY v0

This is an explicitly authorized isolated canary lane. It does NOT mean the full production `/w` swarm is operational.

Load:
- https://juanmanuelpm.github.io/prometeo/.well-known/prometeo.json
- global constitution from that entry before material writes
- https://raw.githubusercontent.com/JuanManuelPM/prometeo/main/coordination/opportunities/CANARY_QUEUE_V0.json

Then:
1. Inspect current claim files under coordination/opportunities/claims/.
2. Choose the highest-priority compatible READY opportunity not already claimed.
3. Atomically claim it by GitHub CREATE of coordination/opportunities/claims/<opportunity_id>.json. Never UPDATE/overwrite a claim. If the create loses a race, reload and try the next item.
4. Create coordination/opportunities/runs/<opportunity_id>/<run_id>.json with STARTED before substantive work.
5. Execute only the opportunity's mission/read scope. Shared runtime/authority/architecture heads are off-limits.
6. Create coordination/opportunities/returns/<opportunity_id>/<run_id>.json with the evidence-backed result, then update only your own run to DONE with return_ref.
7. Reload queue + claims. If another compatible unclaimed READY opportunity remains and continuing is safe/useful, you may claim ONE additional item and repeat. Otherwise stop with IDLE_NO_SAFE_WORK.
8. Never ask the human to copy results to the parent. The parent reads durable returns.

O-BIG09-RECOVERY-R1 and O-BIG13-RECOVERY-R1 are parent-authorized independent read-only recovery attempts. The old A attempts remain preserved; if they return later, both results are candidate evidence.

Goal of this canary: several fresh chats can receive this exact same prompt and naturally spread across distinct useful work without manual numbering.
