{
  "schema": "prometeo.page-thread-bridge-contract/v1",
  "status": "CANDIDATE",
  "opportunity_id": "O-SWARM-PAGE-THREAD-BRIDGE-V1",
  "purpose": "Materialize the missing executable bridge from page/surface-scoped human intent to preserve-first planner input, a deduplicated worker opportunity candidate, a durable sanitized RETURN feed and a Universal Host reopen route.",
  "discovery": {
    "existing_page_change_thread_module_found": false,
    "registry_state": "BRIDGE_CANDIDATE_PENDING",
    "registry_ref": "coordination/swarm-v1/SURFACE_REGISTRY_CANDIDATE_V1.json",
    "execution_protocol_ref": "coordination/AGENT_EXECUTION_PROTOCOL_V1.md",
    "note": "Page Change Thread existed as a protocol/projection concept, not as an executable bridge module. This candidate materializes that missing layer without creating a second product authority."
  },
  "inputs": {
    "target": ["surface_id", "page_id|null", "owner_ref"],
    "intent": ["raw_text (private input only)", "sanitized_summary", "public_safe_summary=true", "capture_refs"],
    "context": ["current refs (required)", "candidate refs", "history refs"],
    "routing": ["host_url", "host_project", "return_root"]
  },
  "outputs": [
    "stable Page Change Thread pointer",
    "preserve-first local planner input",
    "deterministic/deduplicated opportunity candidate",
    "execution packet seed metadata (not an issued capability)",
    "sanitized RETURN feed pointer",
    "Universal Host reopen URL"
  ],
  "invariants": [
    "raw human intent is never emitted in public coordination output",
    "private Capture literals are never emitted",
    "Current, Candidate and History remain distinct",
    "at least one exact current evidence ref is required",
    "same target+intent+evidence yields the same work identity",
    "surface-only targets do not invent a page id",
    "owner resolution remains required before mutation",
    "bridge output never implies Human Accepted or Served",
    "bridge output never grants global promotion authority"
  ],
  "authority": {
    "class": "CANDIDATE_ONLY_NO_GLOBAL_PROMOTION",
    "mutation": "SCOPED_OWNER_ONLY",
    "promotion_authority": "NONE",
    "page_change_thread_role": "HUMAN_PROJECTION_AND_EXECUTION_CONTINUITY",
    "product_authority": false
  },
  "implementation_ref": "scripts/page-change-thread-bridge.mjs",
  "test_ref": "tests/page-change-thread-bridge-v1.test.mjs"
}
