{
  "schema": "prometeo.agent-runtime/v2",
  "version": 2,
  "status": "COORDINATION_RUNTIME_NOT_PRODUCT_AUTHORITY",
  "purpose": "Make disposable parallel chats behave like coordinated Prometeo workers with minimal repeated reasoning.",
  "stable_entry": "https://juanmanuelpm.github.io/prometeo/.well-known/prometeo.json",
  "public_runtime_manifest": "https://juanmanuelpm.github.io/prometeo/agent-runtime/manifest.json",
  "authority_rule": "Runtime coordinates work only. Current, Catalog, Lineage, Human Accepted and Served keep their existing owners.",
  "hard_commands": {
    "Prometeo": [
      "Resync before answering the unfinished human intent.",
      "Load the stable entry and current runtime manifest.",
      "Infer the current workstream from the conversation; do not ask the human to repeat context when it can be recovered.",
      "Load exactly one compiled Work Packet, then only follow conditional references that can change the current decision.",
      "Recover branch drift from the packet activity delta rather than restarting repository archaeology.",
      "Choose the execution profile from this contract and continue the last materially unsatisfied request.",
      "End with a Prometeo Seal only if the runtime and packet were actually loaded."
    ],
    ".": [
      "If a Prometeo workstream is already active, continue its current execution frontier without regenerating context/maps/specification.",
      "If no runtime packet is active, treat dot as a request to resync Prometeo before continuing the previously agreed next action."
    ]
  },
  "compiled_context_rule": "Agents should consume compiled Work Packets before raw PACK/NOW/DELTA files. Raw owners remain available for drift, ambiguity or authority questions.",
  "context_layers": {
    "included": ["workstream identity", "human intent", "already-decided constraints", "last return", "relevant global deltas", "branch activity delta", "relevant upstream main activity", "high-value targets", "known failures", "conditional read graph", "execution profile rules", "seal state"],
    "conditional": ["Current/Catalog/Lineage when authority affects the decision", "Input Ownership when changing gestures", "Context Foundry when changing memory", "Release contracts only when crossing release boundaries", "historical sources only when a contradiction cannot be resolved from recent state"],
    "forbidden_public_compilation": ["private transcripts", "private Patent snapshots", "credentials", "LOCAL context", "token-gated user data"]
  },
  "seal": {
    "active": "🟣 P✓",
    "human_or_external_boundary": "🟡 P~",
    "runtime_or_authority_failure": "🔴 P!",
    "rule": "Never print 🟣 P✓ unless the current stable entry and one compiled packet were successfully loaded during this work cycle.",
    "minimal_format": "🟣 P✓ · <WORKSTREAM>",
    "diagnostic_format": "🟣 P✓ · <WORKSTREAM> · <PROFILE> · R2 · Δ<GLOBAL_REVISION>"
  },
  "execution_profile_owner": "coordination/EXECUTION_PROFILES.json",
  "compiler": "scripts/build-agent-runtime.mjs",
  "publication": ".github/workflows/agent-runtime-v2.yml",
  "efficiency_law": "Maximize durable useful delta per turn: useful product change + future work eliminated + reusable learning + human friction removed - repeated reasoning - coordination overhead - verification loops.",
  "return_rule": "RETURN is an accelerator, not authority. If missing or stale, reconstruct only the small Git delta since the last known head."
}
