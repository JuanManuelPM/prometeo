# Prometeo Active Method vNext — Candidate v2

Status: **CANDIDATE — FINAL BIGPIC COVERAGE 18/18**  
Owner: `chat-object-prometeo-chat-control-main`  
Evidence gate: `B_BIGPIC_01_FINAL_CONVERGENCE_V1.json`

This candidate composes `ACTIVE_METHOD_VNEXT_DRAFT_V1.md` with the final Steward/Merger and learning/falsification contracts from BIG-09/BIG-13 recovery evidence. It does not replace the Global Constitution and is not yet a promoted METHOD_HEAD.

## Inherited method

Preserve Draft v1's method authority stack, clause registry/supersession model, compilation law, Reconstruct -> Map -> Develop -> Challenge -> Gate -> Execute -> Critique/Repair/Retest -> Integrate/Continue sequence, FAST/DEEP/EXHAUSTIVE/CONTINUE profiles, deep campaign program, parallel-worker method, progressive context method, successor proof and governed self-improvement.

## Integration method finalization

After worker RETURN:
1. verify immutable return identity/hash/run linkage and declared scope;
2. extract semantic atoms without erasing original evidence;
3. attach scope, authority, provenance, freshness and evidence sufficiency;
4. deduplicate compatible semantics while preserving all source edges;
5. create explicit contradiction edges for same-scope incompatibilities;
6. run pre-decision coverage audit;
7. owning Steward/Merger adjudicates each scoped cluster;
8. persist candidate integration decision + rejected/deferred alternatives;
9. record append-only, scope-aware consumption ledger entries;
10. run post-decision coverage + relevant falsifiers;
11. only then enter separately authorized host/owner mutation path;
12. persist mutation and post-write verification receipts separately.

Never infer promotion from DONE, recency, confidence, majority, latest commit or consumption. Human Accepted, Current and Served remain separate.

## Incident -> prevention method

When a material failure or expectation violation occurs:
1. CAPTURE INCIDENT: expected vs observed, evidence, scope, authority stage, impact;
2. VERIFY/CLASSIFY: distinguish launch, execution, integration, authority, served, context/source and other failure classes;
3. NORMALIZE NEGATIVE PHENOTYPE: trigger + observable signature + violated invariant;
4. FORM CAUSAL HYPOTHESES with discriminating tests;
5. EMIT scoped Vaccine/detector/recovery candidates plus at least one Falsification Case;
6. REPRODUCE before/after where safe;
7. run preserve/regression and negative-control/mutation tests;
8. parent/steward integrates candidate through the correct authority path;
9. compile relevant ACTIVE vaccines/negative knowledge/falsifiers into future work;
10. WATCH RECURRENCE and reopen the model if prevention escapes.

A Golden is promoted only from reproducible bounded positive behavior with evidence, allowed variance and a regression comparison capable of catching a materially wrong result.

## Mandatory falsification additions

Before METHOD_HEAD or major architecture/method promotion, include tests for:
- integration idempotency and no double-promotion after steward recovery;
- late original return after a recovery return;
- partial-scope contradictions;
- Current-head drift immediately before mutation;
- worker DONE without durable RETURN;
- allegedly immutable RETURN bytes changing after first consumption;
- Vaccine mutation/removal causing expected regression/falsifier failure;
- recurrence of a known negative phenotype after ACTIVE prevention;
- Golden/reference ability to distinguish a wrong output, not merely reproduce itself;
- regression-suite selection determinism and applicability.

## Strategic non-regression and self-critique

At every material planning/integration frontier, the parent/Planner must:
- check north-star drift;
- identify the multiplicative throughput bottleneck;
- compare at least two plausible routes for important decisions;
- prefer work that improves future work-generation/integration/context/recovery when risk/cost is comparable;
- inspect User Intent Gap Audit, incidents/negative phenotypes and critic findings;
- trigger a fresh independent critic on high-impact promotion, repeated drift, evidence conflict, persistent blocker or stalled throughput;
- preserve the stronger existing plan when a new local human message is merely a bounded delta.

Human goals/preferences/acceptance are authoritative where appropriate; human implementation guesses are not the ceiling on Prometeo's planning quality.

## Verification receipts

Do not call a mechanism operational without durable proof of use.

Material workers should leave:
- claim/run/worker IDs;
- source head/epoch;
- mission + acceptance criteria;
- context/read receipt;
- checkpoints reached;
- output/evidence refs;
- RETURN/DONE lineage;
- post-return action/proposals.

Material Planner/parent generations should leave:
- generation + parent generation;
- north-star ref;
- bottleneck;
- evidence/returns/proposals/incidents/critic inputs consumed;
- alternatives considered;
- chosen route;
- generated opportunities;
- critic decision;
- next-generation trigger;
- human action only if a real boundary remains.

## Work-depth rule

One worker may continue through multiple sequential checkpoints when context, authority, failure domain and write ownership stay coherent. Split when independence, parallelism, fresh criticism, authority separation, context freshness, collision isolation or failure isolation creates real value. Do not split merely to increase chat count; do not stop merely because prose was produced.

## Context compilation direction

For scalable context, compile:
- L0 Cognitive Kernel: north star, active method, root identity, authority, current frontier;
- L1 domain/folder digest(s): key artifacts, decisions, unresolved conflicts, freshness and pointers;
- L2 exact source evidence only when needed.

Use hierarchical manifests + typed graph + structured/lexical/vector discovery as derived aids. Emit a context receipt proving which sources were actually selected and what high-relevance material was omitted. Authority/currentness never comes from vector similarity.

## Current method frontier

The next method improvement is not another broad architecture essay. It is executable proof of recursive metabolism:

`returns/proposals/incidents/critic findings -> Planner -> dependency-aware READY Queue -> identical workers -> durable returns -> Steward -> next Planner generation`

Promotion toward production `/w` requires demonstrated collision handling, context receipts, stale/retry/supersession, verification, critic cadence and at least two automatic generations without the human designing the second batch.
