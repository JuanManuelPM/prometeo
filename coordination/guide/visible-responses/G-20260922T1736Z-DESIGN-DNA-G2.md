Quedó hecho y ya no depende de este chat.

- Entré al Mesh como **GUIDE-2** y publiqué las dos directivas pendientes: **GUIDE-2 coordina / GUIDE-1 queda secundario en UI-TV**, y **Design DNA pasa a ser obligatorio**. El claim `guide/design-dna` quedó `FINISHED`.
- Creé `coordination/design-dna/` con el Canon, **20 invariantes**, **15 failure vaccines**, Goldens/anti-Goldens, Preservation Contract y el regression harness.
- Lo cableé al arranque y a la autoridad real: `p.txt` **v4.10.0**, `.well-known/prometeo.json`, Global Constitution, Continuity Head, Guide Succession, Chat Object, Current Mission y Guide Writeback.
- La regla permanente ahora es: **READ DNA → identificar invariant/vaccine/Golden → Preservation Contract → cambio aislado → comparación contra baseline → promoción sólo con evidencia → actualizar lineage/DNA si cambió la lección**.
- El baseline histórico simple quedó protegido. Los números de `POOL-8-100-01` y `GRID-5-100` siguen explícitamente como **CANDIDATE_EVIDENCE** hasta recuperar receipts; `CATLAB-P2B-01` sí queda como evidencia durable verificada.
- El experimento `GRID-10X100X2-V1` ya quedó definido como harness permanente con A/B/C, fatiga, muerte+rescue, reencarnación, concurrencia, blind taskset y métricas de calidad/duplicados/pérdida/cross-grid/latencia/intervención humana.

**Verificación:** todos los JSON nuevos fueron releídos desde `main` y parsean; el branch `gh-pages` ya contiene `p.txt` v4.10.0 con el guard y `.well-known/prometeo.json` con `design_dna`. No declaro verificación HTTP de bytes servidos porque el navegador de verificación no pudo leer esa URL en esta sesión.

Lo único que queda deliberadamente **no promovido** es la implementación ejecutable del harness: `REGRESSION_HARNESS_V1` está en `SPEC_ACTIVE_IMPLEMENTATION_CANDIDATE`. El próximo punto puede dedicarlo a cablear y correr el experimento, ya bajo estas reglas.
