(()=>{'use strict';
const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)],esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const src=new URLSearchParams(location.search).get('instance')||'./study-library/instances/modelos-teorias-ii-p1-v2.json';let I,moduleId,openTopic,theme=0,mastery={},practiceDone={},drawer=false;
const key=n=>`study:v2:${I?.instance_id||'pending'}:${n}`,save=(n,v)=>localStorage.setItem(key(n),JSON.stringify(v)),read=(n,f)=>{try{return JSON.parse(localStorage.getItem(key(n))||'')??f}catch{return f}};
const topics=()=>I.modules.flatMap(m=>m.topics),doneCount=()=>topics().filter(t=>mastery[t.id]).length,pct=()=>Math.round(doneCount()/Math.max(1,topics().length)*100),source=id=>I.sources.find(s=>s.id===id);
const bid=id=>`${I.instance_id}--${id}`,thumb=id=>localStorage.getItem(`study:v2:modelos-teorias-ii:whiteboard-v10:thumb:${bid(id)}`)||'';
function applyTheme(){const ts=I.themes?.length?I.themes:[{background:'#F5DABF',ink:'#6C151E'}],t=ts[theme%ts.length];document.documentElement.style.setProperty('--bg',t.background);document.documentElement.style.setProperty('--ink',t.ink);q('meta[name="theme-color"]')?.setAttribute('content',t.background);save('settings',{theme})}
function refs(ids=[]){return `<div class="sourceRefs">${ids.map(id=>`<button data-source="${esc(id)}">${esc(source(id)?.title||id)}</button>`).join('')}</div>`}
function recall(t){return t.recall_atoms?.length?`<div class="recall">${t.recall_atoms.map(a=>`<div class="recallRow"><div><div class="recallCue">${esc(a.cue)}</div><div class="recallAnswer">${esc(a.answer)}</div></div><button data-recall>ver</button></div>`).join('')}</div>`:''}
function topicHTML(t,i){const on=t.id===openTopic,im=thumb(t.id),done=!!mastery[t.id];return `<article class="topic ${on?'open':''}" data-topic="${esc(t.id)}" data-study-wb-id="${esc(bid(t.id))}"><div class="topicHead" data-open-topic="${esc(t.id)}"><span class="topicIndex">${String(i+1).padStart(2,'0')}</span><span><span class="topicTitle">${esc(t.title)}</span><span class="topicAnchor">${esc(t.anchor)}</span></span><span class="topicTools"><button class="masterBtn ${done?'done':''}" data-master="${esc(t.id)}">${done?'✓':'recordar'}</button><button class="wbBtn ${im?'hasDrawing':''}" data-wb="${esc(t.id)}" title="Pizarrón">${im?`<img src="${im}" alt="">`:''}<span>${im?'':'pizarrón'}</span></button><button class="openBtn" data-open-topic="${esc(t.id)}">+</button></span></div><div class="topicBody"><div class="reading"><p class="lead">${esc(t.summary)}</p><p class="explanation">${esc(t.explanation)}</p><div class="paneGrid">${t.relations?`<section class="pane"><div class="paneLabel">Relación</div><div class="paneText">${esc(t.relations)}</div></section>`:''}${t.do_not_confuse?`<section class="pane"><div class="paneLabel">No confundir</div><div class="paneText">${esc(t.do_not_confuse)}</div></section>`:''}${t.exam_use?`<section class="pane"><div class="paneLabel">En examen</div><div class="paneText">${esc(t.exam_use)}</div></section>`:''}</div>${refs(t.source_refs)}${recall(t)}</div></div></article>`}
function maps(){return (I.transversal_maps||[]).map(m=>`<section class="map"><header><b>${esc(m.title)}</b><span>${esc(m.relation||'')}</span></header><div class="mapGroups">${m.groups.map(g=>`<article class="mapGroup"><b>${esc(g.title)}</b><p>${esc(g.summary)}</p></article>`).join('')}</div></section>`).join('')}
function moduleHTML(){const m=I.modules.find(x=>x.id===moduleId)||I.modules[0];moduleId=m.id;return `<section class="module"><header class="moduleHead"><div><small>${esc(m.id.toUpperCase())}</small><h1 class="moduleTitle">${esc(m.title)}</h1></div><p class="moduleSummary">${esc(m.summary)}</p></header><div class="topics">${m.topics.map(topicHTML).join('')}</div>${maps()}</section>`}
function rail(){return `<nav class="rail">${I.modules.map((m,i)=>`<button data-module="${esc(m.id)}" class="${m.id===moduleId?'on':''}"><small>${String(i+1).padStart(2,'0')}</small><b>${esc(m.title)}</b></button>`).join('')}</nav>`}
function countdown(){if(!I.exam?.datetime)return'';const ms=new Date(I.exam.datetime)-Date.now();if(!Number.isFinite(ms)||ms<=0)return'';return `<span class="countdown">${Math.floor(ms/86400000)}d ${Math.floor(ms%86400000/3600000)}h</span>`}
function practiceHTML(){return (I.practice||[]).map(p=>`<article class="practiceItem" data-practice="${esc(p.id)}"><small>${esc(p.type)}</small><h3>${esc(p.prompt)}</h3><div class="practiceActions"><button data-reveal>respuesta</button><button data-pdone="${esc(p.id)}" class="${practiceDone[p.id]?'done':''}">${practiceDone[p.id]?'hecho':'marcar'}</button></div><div class="practiceAnswer">${p.answer?`<p>${esc(p.answer)}</p>`:''}${p.rubric?.length?`<ul>${p.rubric.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:''}${refs(p.source_refs)}</div></article>`).join('')}
function sourceSheet(){return I.sources.map(s=>`<div class="sourceRow" data-source-row="${esc(s.id)}"><small>${esc(s.role)}</small><div><b>${esc(s.title||s.id)}</b>${s.notes?`<p>${esc(s.notes)}</p>`:''}</div></div>`).join('')}
function render(){const p=pct();q('#app').innerHTML=`<div class="app"><header class="top"><div class="identity"><b>${esc(I.course)}</b><span>${esc(I.exam?.label||'')}</span></div><div class="topRight"><div class="progressWrap"><div class="progressTrack"><div class="progressFill" style="width:${p}%"></div></div><span class="progressText">${p}% · ${doneCount()}/${topics().length}</span></div>${countdown()}<button class="sourceBtn" id="sources">fuentes</button><button class="themeBtn" id="theme" title="Tema"></button></div></header><div class="layout">${rail()}<main class="study">${moduleHTML()}</main></div><div class="practiceDock"><button class="practiceBtn" id="practice">práctica · ${Object.values(practiceDone).filter(Boolean).length}/${I.practice?.length||0}</button></div><aside class="drawer ${drawer?'open':''}"><header class="drawerHead"><b>Práctica</b><button id="closePractice">×</button></header><div class="practiceList">${practiceHTML()}</div></aside><div class="sourceOverlay" id="sourceOverlay"><div class="sourceSheet"><header><b>Fuentes</b><button id="closeSources">×</button></header>${sourceSheet()}</div></div><div class="boardOverlay" id="board"><iframe title="Pizarrón" allow="clipboard-read; clipboard-write"></iframe></div></div>`;wire()}
function openBoard(id){const o=q('#board'),f=q('iframe',o);f.src=`./study-system-v2-whiteboard-v10.html?topic=${encodeURIComponent(bid(id))}&v=generic2`;o.classList.add('open')}
function showSource(id){const over=q('#sourceOverlay'),row=q(`[data-source-row="${CSS.escape(id)}"]`);over.classList.add('open');setTimeout(()=>row?.scrollIntoView({block:'center'}),20)}
function wire(){qa('[data-module]').forEach(b=>b.onclick=()=>{moduleId=b.dataset.module;openTopic=null;render()});qa('[data-open-topic]').forEach(b=>b.onclick=e=>{if(e.target.closest('[data-master],[data-wb]'))return;const id=b.dataset.openTopic;openTopic=openTopic===id?null:id;render()});qa('[data-master]').forEach(b=>b.onclick=e=>{e.stopPropagation();mastery[b.dataset.master]=!mastery[b.dataset.master];save('mastery',mastery);render()});qa('[data-wb]').forEach(b=>b.onclick=e=>{e.stopPropagation();openBoard(b.dataset.wb)});qa('[data-recall]').forEach(b=>b.onclick=()=>b.closest('.recallRow').classList.toggle('revealed'));q('#practice').onclick=()=>{drawer=true;q('.drawer').classList.add('open')};q('#closePractice').onclick=()=>{drawer=false;q('.drawer').classList.remove('open')};qa('[data-reveal]').forEach(b=>b.onclick=()=>b.closest('.practiceItem').classList.toggle('reveal'));qa('[data-pdone]').forEach(b=>b.onclick=()=>{practiceDone[b.dataset.pdone]=!practiceDone[b.dataset.pdone];save('practice',practiceDone);render()});q('#theme').onclick=()=>{theme=(theme+1)%Math.max(1,I.themes?.length||1);applyTheme();render()};q('#sources').onclick=()=>q('#sourceOverlay').classList.add('open');q('#closeSources').onclick=()=>q('#sourceOverlay').classList.remove('open');q('#sourceOverlay').onclick=e=>{if(e.target.id==='sourceOverlay')e.currentTarget.classList.remove('open')};qa('[data-source]').forEach(b=>b.onclick=e=>{e.preventDefault();showSource(b.dataset.source)})}
window.addEventListener('message',e=>{if(e.origin!==location.origin||!e.data)return;if(e.data.type==='study-wb7-close'){const o=q('#board');o?.classList.remove('open');setTimeout(()=>{const f=q('iframe',o);if(f)f.src='about:blank';render()},180)}if(e.data.type==='study-wb10-thumb')setTimeout(render,50)});
async function init(){try{const r=await fetch(src,{cache:'no-store'});if(!r.ok)throw Error(`${r.status} ${src}`);I=await r.json();if(I.schema!=='prometeo.study.exam/v2')throw Error('schema');moduleId=I.modules?.[0]?.id;theme=Number(read('settings',{theme:0}).theme||0);mastery=read('mastery',{});practiceDone=read('practice',{});applyTheme();render()}catch(e){q('#app').innerHTML=`<div class="fatal"><b>No pude cargar la instancia.</b><code>${esc(String(e.message||e))}</code></div>`}}
init();
})();