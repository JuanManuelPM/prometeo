import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.57.4";
const SB_URL=Deno.env.get("SUPABASE_URL")!, SERVICE=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const db=createClient(SB_URL,SERVICE,{auth:{persistSession:false}});
const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"content-type","Access-Control-Allow-Methods":"GET,POST,OPTIONS","Cache-Control":"no-store"};
const out=(x:any,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{...CORS,"Content-Type":"application/json"}});
async function sha256(v:string){const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function load(workId:string,token:string){if(!/^[0-9a-f-]{36}$/i.test(workId)||token.length<30)throw new Error("INVALID_CLAIM");const {data,error}=await db.from("creator_external_work").select("*").eq("id",workId).maybeSingle();if(error||!data)throw new Error("WORK_NOT_FOUND");if(!data.claim_token_hash||(await sha256(token))!==data.claim_token_hash)throw new Error("INVALID_CLAIM");return data}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:CORS});try{const requestUrl=new globalThis.URL(req.url);if(req.method==="GET"&&requestUrl.searchParams.get("schema")==="1")return out({schema:"prometeo.creator-external-work/v1",get:"GET ?work_id=<uuid>&token=<opaque>",return:"POST {work_id,token,result}",rule:"The token grants one work packet only. A valid return is applied automatically if the story version still matches."});if(req.method==="GET"){const workId=requestUrl.searchParams.get("work_id")||"",token=requestUrl.searchParams.get("token")||"",w=await load(workId,token);if(["RETURNED","APPLIED","CANCELLED"].includes(w.status))return out({ok:true,work:{id:w.id,status:w.status,task:w.task,context_version:w.context_version,result:w.result||null}});if(w.status==="OPEN")await db.from("creator_external_work").update({status:"CLAIMED",claimed_at:new Date().toISOString()}).eq("id",w.id).eq("status","OPEN");return out({ok:true,schema:"prometeo.creator-work-packet/v1",work:{id:w.id,status:"CLAIMED",task:w.task,context_version:w.context_version,context:w.context},return_contract:{method:"POST",url:`${SB_URL}/functions/v1/creator-work`,body:{work_id:w.id,token:"<same opaque token>",result:{summary:"string",changes:"object",proposals:"array",notes:"array"}}}})}if(req.method==="POST"){const b=await req.json(),workId=String(b?.work_id||""),token=String(b?.token||""),w=await load(workId,token);if(w.status==="APPLIED")return out({ok:true,idempotent:true,work:{id:w.id,status:w.status}});if(w.status==="CANCELLED")return out({ok:false,error:"WORK_CANCELLED"},409);if(b?.result==null||typeof b.result!=="object"||Array.isArray(b.result))return out({ok:false,error:"RESULT_OBJECT_REQUIRED"},400);if(JSON.stringify(b.result).length>250000)return out({ok:false,error:"RESULT_TOO_LARGE"},413);if(w.status!=="RETURNED"){const {error}=await db.from("creator_external_work").update({status:"RETURNED",result:b.result,returned_at:new Date().toISOString()}).eq("id",w.id).in("status",["OPEN","CLAIMED"]);if(error)throw error}const {data:applied,error:ae}=await db.rpc("creator_apply_external_work",{p_owner:w.owner_id,p_work:w.id});if(ae){const msg=String(ae.message||ae);if(msg.includes("STORY_VERSION_CONFLICT"))return out({ok:false,error:"STORY_VERSION_CONFLICT",work_id:w.id},409);throw ae}return out({ok:true,work:{id:w.id,status:"APPLIED"},apply:applied})}return out({ok:false,error:"METHOD_NOT_ALLOWED"},405)}catch(e){const m=String((e as any)?.message||e);return out({ok:false,error:m},m.includes("NOT_FOUND")?404:m.includes("INVALID")?403:500)}});