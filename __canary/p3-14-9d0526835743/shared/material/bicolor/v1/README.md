# Prometeo Bicolor Tactile Material v1

Shared physical owner extracted from the accepted two-color control language used by Calendar. Consumers provide only `--a` and `--b`; controls use those same two colors for surface, text, border and depth.

Use `.p-bi-control` plus `data-p-touch`. Content surfaces should remain flat unless they are manipulated.
