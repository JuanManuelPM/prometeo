import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const root=new URL('../',import.meta.url).pathname;
const listeners={};let nav={state:'IDLE',currentNode:'random',path:['random'],selectedIndex:4,selected:null,paletteOffset:0,history:[{node:'__home__',selectedIndex:0}]};
const pageCard={dataset:{cardId:'page-x'},querySelector:s=>s==='.terminal'||s==='.return-tooth'||s==='.terminal-frame'?{}:null};
const doc={body:{},addEventListener:(t,f)=>((listeners[t]??=[]).push(f)),removeEventListener(){},getElementById:id=>id==='worldStage'?{}:null,querySelectorAll:s=>s.includes('data-node-kind="page"')&&nav.currentNode==='page-x'?[pageCard]:[],querySelector:s=>s.includes('data-node-kind="page"')&&nav.currentNode==='page-x'?pageCard:null};
const ctx={console,structuredClone,Date,Math,JSON,setTimeout,clearTimeout,queueMicrotask,document:doc,MutationObserver:class{observe(){}disconnect(){}},globalThis:null,addEventListener(){},removeEventListener(){}};ctx.globalThis=ctx;ctx.window=ctx;
let focus=[],seq=0;ctx.PrometeoOwnership={snapshot:()=>({focus:[...focus]}),acquireFocus:(owner,opt={})=>{const parent=focus.at(-1);if(parent&&opt.parentLeaseId!==parent.id)throw Object.assign(new Error('parent'),{code:'PROMETEO_FOCUS_PARENT_REQUIRED'});const t={id:`f${++seq}`,owner,...opt};focus.push(t);return t},releaseFocus:t=>{if(focus.at(-1)?.id!==t.id)throw Object.assign(new Error('order'),{code:'PROMETEO_FOCUS_LEASE_ORDER'});focus.pop();return{released:true}},acquireGesture:(owner,opt)=>({id:'g1',owner,...opt}),releaseGesture:()=>true};
let record=null;ctx.PrometeoPersistence={readRecord:()=>record,write:(ns,id,data,{baseRevision=0}={})=>{const current=record?.revision||0;if(baseRevision!==current)throw Object.assign(new Error('stale'),{code:'PROMETEO_STALE_STATE_WRITE'});record={data:structuredClone(data),revision:current+1};return{revision:current+1}},read:()=>record?.data||null};
const route={'__home__':{0:['prometeo',['prometeo']]},'prometeo':{1:['projects',['prometeo','projects']]},'projects':{2:['page-x',['prometeo','projects','page-x']]}};
ctx.__PROMETEO_BUILD__='PROMETEO_V53_COMPLETE_EXAMPLE_ATLAS_20260901';
ctx.__PROMETEO_V53__={getState:()=>structuredClone(nav),home:()=>nav.currentNode==='__home__',left:()=>{if(!nav.history.length){nav={...nav,currentNode:'__home__',path:[],selectedIndex:0,history:[]};return;}const s=nav.history.pop();nav={...nav,currentNode:s.node,path:s.path||[],selectedIndex:s.selectedIndex||0,history:[...nav.history]};},right:()=>{const dest=route[nav.currentNode]?.[nav.selectedIndex];if(!dest)return;const old={node:nav.currentNode,path:[...nav.path],selectedIndex:nav.selectedIndex,paletteOffset:0};nav.history.push(old);nav={...nav,currentNode:dest[0],path:dest[1],selectedIndex:0,history:[...nav.history]};},up:()=>{nav.selectedIndex=Math.max(0,nav.selectedIndex-1)},down:()=>{nav.selectedIndex++}};
vm.createContext(ctx);vm.runInContext(fs.readFileSync(root+'shared/shell/v53-adapter/v1/v53-shell-adapter.js','utf8'),ctx);
nav={state:'IDLE',currentNode:'__home__',path:[],selectedIndex:0,selected:'prometeo',paletteOffset:0,history:[]};
const target={schema:'prometeo.v53-semantic-return/v1',build:ctx.__PROMETEO_BUILD__,currentNode:'page-x',path:['prometeo','projects','page-x'],selectedIndex:0,history:[{node:'__home__',selectedIndex:0,paletteOffset:0},{node:'prometeo',selectedIndex:1,paletteOffset:0},{node:'projects',selectedIndex:2,paletteOffset:0}]};
const restored=await ctx.PrometeoV53ShellAdapter.restoreSemantic(target);assert.equal(restored.currentNode,'page-x');assert.equal(restored.history.length,3);
const shell=ctx.PrometeoV53ShellAdapter.mount({persist:true});shell.reconcile();assert.equal(focus.length,1);assert.equal(focus[0].owner,'v53.terminal');shell.persistNow();const rev=record.revision;await new Promise(r=>setTimeout(r,2));shell.persistNow();assert.equal(record.revision,rev,'capturedAt must not create semantic write churn');
const nested=ctx.PrometeoOwnership.acquireFocus('pagekit',{parentLeaseId:focus.at(-1).id});assert.throws(()=>shell.destroy(),e=>e.code==='PROMETEO_SHELL_DESTROY_WITH_NESTED_FOCUS');ctx.PrometeoOwnership.releaseFocus(nested);
nav.currentNode='projects';shell.reconcile();assert.equal(focus.length,0);shell.destroy();
console.log(JSON.stringify({ok:true,tests:['semantic-route-replay','no-pixel-restore','terminal-focus-acquire-release','semantic-fingerprint-no-write-churn','nested-focus-destroy-fails-closed']},null,2));