# Creator canary fixture

`vertical-144x256.mp4` is a one-second H.264/AAC vertical MP4 generated only for deterministic Creator Runtime canaries. It is not production content and must never be reported as AI-generated output.

The Creator worker fetches this exact committed binary from the work branch for zero-cost end-to-end pipeline tests.
