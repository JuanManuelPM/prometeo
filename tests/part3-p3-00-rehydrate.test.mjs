import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {fileURLToPath} from 'node:url';
import fs from 'node:fs';
const ROOT=fileURLToPath(new URL('../',import.meta.url));const run=spawnSync(process.execPath,['scripts/p3-00-rehydrate.mjs'],{cwd:ROOT,encoding:'utf8'});if(run.status!==0){process.stderr.write(run.stderr||'');process.stderr.write(run.stdout||'');process.exit(run.status||1)}const e=JSON.parse(run.stdout);assert.equal(e.schema,'prometeo.p3-00-evidence/v1');assert.equal(e.gate,'P3-00');assert.equal(e.generated_from_chat_memory,false);assert.equal(e.ledger.ok,true);assert.ok(e.ledger.count>=4);assert.equal(e.ledger.last_receipt_id,e.state_crosscheck.current_last_receipt);assert.equal(e.state_crosscheck.current_last_receipt,e.state_crosscheck.dot_last_receipt);assert.equal(e.state_crosscheck.head_receipt,e.state_crosscheck.candidate_receipt);assert.equal(e.state_crosscheck.catalog_identity,'catalog-v1:31cb2fefb32e2ccda67100ec7e872c3e3c2a5b61+4d471d2721b3ead0bf5b00c3896fdd5abc79b348');assert.equal(e.state_crosscheck.v53_source,'gitblob:7ca5f3e223ca843e3f9e4b7be1e53b5b65dd3418');assert.equal(e.wake.schema,'prometeo.wake-packet/v1');assert.equal(e.wake.CURRENT.head,'part2-workstream');assert.equal(e.wake.CURRENT.candidate,'part2-workstream');assert.equal(e.wake.CURRENT.visible_frontend,'navigator-v53-visible');assert.ok(String(e.wake.CURRENT.phase).startsWith('PART3'));assert.equal(e.wake.ACTIVE_FRONTEND,'navigator-v53-visible');assert.equal(e.wake.PAGES.count,31);assert.equal(e.wake.PAGES.identity,e.state_crosscheck.catalog_identity);assert.deepEqual(e.wake.HUMAN_ACCEPTED_SCOPES,[{scope:'human_accepted_physics',artifact_id:'navigator-v23-physics'}]);assert.equal(e.wake.SERVED_INHERITED.evidence_state,'INHERITED_PREEXISTING_NOT_PART2_BROWSER_REVERIFIED');assert.equal(e.wake.PENDING.length,1);assert.equal(e.wake.PENDING[0].id,'PENDING-PART3-001');assert.equal(e.wake.PENDING[0].state,'READY');assert.ok(e.wake.RULES.includes('Candidate != Human Accepted != Served.'));assert.equal(e.source_identities.required.length,13);assert.ok(e.source_identities.required.some(x=>x.role==='operator_contract'));for(const s of [e.source_identities.bootstrap,...e.source_identities.required,...e.source_identities.runtime])assert.match(s.git_blob_sha,/^[0-9a-f]{40}$/);assert.match(e.source_identities.branch,/^candidate\//);
const matrix=JSON.parse(fs.readFileSync(new URL('../coordination/PART3_GATE_MATRIX.json',import.meta.url),'utf8')),dot=JSON.parse(fs.readFileSync(new URL('../state/DOT_STATE.json',import.meta.url),'utf8'));const p300=matrix.gates.find(g=>g.id==='P3-00'),p301=matrix.gates.find(g=>g.id==='P3-01'),p302=matrix.gates.find(g=>g.id==='P3-02');assert.ok(p300&&p301&&p302);if(p300.status==='PASS'){if(p301.status==='READY'){assert.equal(dot.next_gate,'P3-01_BROWSER_HARNESS')}else if(p301.status==='PASS'&&p302.status==='READY'){assert.equal(p301.receipt_id,'R-P3-01-BROWSER-0006');assert.equal(dot.phase,'PART3_P3_01_COMPLETE');assert.equal(dot.next_gate,'P3-02_FRESH_AGENT_REINCARNATION')}else if(p301.status==='PASS'&&p302.status==='PASS'){assert.equal(p301.receipt_id,'R-P3-01-BROWSER-0006');assert.equal(p302.receipt_id,'R-P3-02-FRESH-0007');assert.ok(/^PART3_P3_\d{2}(?:_|$)/.test(String(dot.phase)));assert.ok(/^P3-\d{2}(?:_|$)/.test(String(dot.next_gate)))}else assert.fail('P3-01 must be READY/PASS and P3-02 READY/PASS after P3-00')}
console.log(JSON.stringify({ok:true,gate:'P3-00',ledger_receipts:e.ledger.count,current_revision:e.state_crosscheck.current_revision,phase:e.wake.CURRENT.phase,branch:e.source_identities.branch}));
